# 共有

## lib.getNearbyPeds

座標のセットの範囲内にあるすべての歩行者の歩行者ハンドルと座標を取得します。

```lua
lib.getNearbyPeds(coords, maxDistance)
```

- coords: `vector3`
  - チェックする座標。
- maxDistance?: `number`
  - チェックする最大距離。
  - デフォルト: `2.0`

戻り値:

- peds: `{ ped: number, coords: vector3 }[]`
