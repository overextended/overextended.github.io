# 共有

## lib.getClosestObject

座標のセットに最も近いオブジェクトのオブジェクトハンドルと座標を取得します。

```lua
lib.getClosestObject(coords, maxDistance)
```

- coords: `vector3`
  - チェックする座標。
- maxDistance?: `number`
  - チェックする最大距離。
  - デフォルト: `2.0`

戻り値:

- object?: `number`
- coords?: `vector3`
