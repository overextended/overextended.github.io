# 共有

## lib.getNearbyObjects

座標のセットの範囲内にあるすべてのオブジェクトのオブジェクトハンドルと座標を取得します。

```lua
lib.getNearbyObjects(coords, maxDistance)
```

- coords: `vector3`
  - チェックする座標。
- maxDistance?: `number`
  - チェックする最大距離。
  - デフォルト: `2.0`

戻り値:

- objects: `{ object: number, coords: vector3 }[]`
