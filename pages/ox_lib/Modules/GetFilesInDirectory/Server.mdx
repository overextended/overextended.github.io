# サーバー

## lib.getFilesInDirectory
指定されたリソースディレクトリ内のすべてのファイルのリストを、ファイル数とともに取得します

```lua
lib.getFilesInDirectory(path, pattern)
```

- path: `string`
    - ファイルをリストするディレクトリへの相対パスまたは絶対パス。
- pattern: `string`
    - ファイルを検索するときにチェックするパターン。

戻り値:
- files: `string[]`
- fileCount: `number`
